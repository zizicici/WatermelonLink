{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://link.watermelonbackup.com/protocol/v1/client-signaling.schema.json",
  "title": "Watermelon Link client-to-server signaling messages",
  "oneOf": [
    {
      "type": "object",
      "additionalProperties": false,
      "required": ["kind", "payload"],
      "properties": {
        "kind": { "const": "relay" },
        "payload": { "type": "string", "maxLength": 20000 }
      }
    },
    {
      "type": "object",
      "additionalProperties": false,
      "required": ["kind"],
      "properties": { "kind": { "enum": ["complete", "cancel"] } }
    }
  ]
}
